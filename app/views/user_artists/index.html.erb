<!-- This page will show all artists that the current_user follows. -->

<!-- <br> render 'form' %><br> -->

<h2 class="page-header">My Artists</h2>
<% if @my_artists.length == 0 %>
<p>It doesn't look like you've followed any artists yet.<br>Get Out, (leave) right now,<br>
It's the end of you and me</p>
<% else %>
  <% @my_artists.each do |my_artist| %>
  <p><%= link_to my_artist.first.name, artist_path(my_artist.first.id) %> -- <%= link_to 'Unfollow', user_user_artist_path(current_user.id, my_artist.first.id), method: :delete, data: { confirm: 'Are you sure?' } %></p>
  <% end %>
<% end %>
